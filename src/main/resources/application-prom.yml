spring:
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/google_api
    username: postgres
    password: postgres

google-api:
  clientId:
  clientSecret:
  refreshToken:
